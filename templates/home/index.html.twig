{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    {% if app.user %}
        <p>je suis connecté</p>
    {% else %}
        <p>je ne suis pas connecté</p>
    {% endif %}

    {{ form_start(form) }}
    {{ form_row(form.email) }}
        <button type="submit">Rechercher</button>
    {{ form_end(form) }}


    {% for user in pagination %}
        <p>{{ user.email }}</p>
    {% endfor %}
    <div class="pagination">
        {{ knp_pagination_render(pagination) }}
    </div>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/Cours/Symfony/symfony_avance_hb/src/Controller/HomeController.php'|file_link(0) }}">src/Controller/HomeController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/Cours/Symfony/symfony_avance_hb/templates/home/index.html.twig'|file_link(0) }}">templates/home/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
